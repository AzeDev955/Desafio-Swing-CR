import "./style.css";

const seleccion = document.getElementById("tipo-evento");
const campoClase = document.getElementById("campos-clase");
const campoActividades = document.getElementById("campos-actividades");

seleccion.addEventListener("change", () => {
  const valor = seleccion.value;
  switch (valor){
    case "clase":
        break;
    case "actividad"
  }
});
